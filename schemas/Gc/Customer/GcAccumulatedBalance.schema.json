{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "GcAccumulatedBalance.schema.json",
    "title": "GcAccumulatedBalance",
    "definitions": {
        "GcAccumulatedBalance": {
            "$id": "#GcAccumulatedBalance",
            "description": "Represents and tracks the aggregated amount remained or owed in certain account which is owned by certain customer for a set of buckets.",
            "type": "object",
            "properties": {},
            "allOf": [
                {
                    "$ref": "../../Core/Entity.schema.json"
                }
            ]
        }
    }
}